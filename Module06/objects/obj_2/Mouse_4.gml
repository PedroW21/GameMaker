/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor


if(global.movable_obj != id) {
	
	global.movable_obj = id;
	global.points++;
	global.actual_obj = string(id);
	return;
	
}